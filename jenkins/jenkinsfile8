pipeline {
    agent any
    triggers {
        cron('*/3 * * * *')  // Run every 3 minutes
    }
    stages {
        stage('Build') {
            steps {
                echo "🔨 Building the project..."
                sh 'date'   // Run the date command properly
            }
        }

        stage('Test') {
            steps {
                echo "🧪 Running tests..."
            }
        }

        stage('Deploy') {
            steps {
                echo "🚀 Deploying application..."
            }
        }
    }
    post {
        always {
            echo "✅ Pipeline finished (success or failure)."
        }
    }
}

